export type ChatDataTypes = {
  chat_created_at: Date;
  chat_id: string;
  content: string;
  message_id: string;
  sender_id: string;
  sent_at: Date;
};

export type ChatsDataTypes = {
  chat_id: string;
  chat_name: string;
};
